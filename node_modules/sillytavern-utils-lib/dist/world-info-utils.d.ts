import { WIEntry } from './types/world-info.js';
export type getActiveWorldInfoInclude = 'all' | 'global' | 'character' | 'chat' | 'persona';
/**
 * @returns Entries by world name. <worldName, entries[]>
 */
export declare function getActiveWorldInfo(include: getActiveWorldInfoInclude[], targetCharacterIndex?: number): Promise<Record<string, WIEntry[]>>;
/**
 * @throws {Error} if entry/world not found
 */
export declare function applyWorldInfoEntry({ entry, selectedWorldName, skipSave, skipReload, operation, }: {
    entry: WIEntry;
    selectedWorldName: string;
    skipSave?: boolean;
    skipReload?: boolean;
    operation?: 'add' | 'update' | 'auto';
}): Promise<{
    entry: WIEntry;
    operation: 'add' | 'update';
}>;
//# sourceMappingURL=world-info-utils.d.ts.map